name: Deploy to Production
on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd /data/ecosystem
            git pull origin main
            docker compose -f docker-compose.yml -f deployments/docker-compose.prod.yml up -d --build
            docker image prune -f

      - name: Health check
        run: |
          sleep 10
          curl -f https://tsbi.fun
          curl -f https://api.tsbi.fun/health
