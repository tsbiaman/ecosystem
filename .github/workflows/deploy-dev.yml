name: Deploy to Development
on:
  push:
    branches: [develop]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd /data/ecosystem
            git pull origin develop
            docker stack deploy -c deployments/stack-tsbi-dev.yml tsbi-dev
            
            # Cleanup old images
            docker image prune -f

      - name: Health check
        run: |
          sleep 10
          curl -f https://dev.tsbi.fun
          curl -f https://api-dev.tsbi.fun/health
